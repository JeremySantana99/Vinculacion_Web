// src/pages/Tours.tsx
import React from 'react';
const Tours: React.FC = () => {
  // Esta página está intencionalmente vacía por ahora — solo muestra el layout (header/footer).
  return (
    <div style={{ padding: '2rem', maxWidth: 1200, margin: '0 auto' }}>
      {/* Si quieres que esté completamente en blanco, deja esto vacío.
          Por ahora dejamos una nota pequeña para saber que la ruta funciona. */}
      <div style={{ color: '#6b7280' }}>Página de Tours (en construcción)</div>
    </div>
  );
};
export default Tours;
